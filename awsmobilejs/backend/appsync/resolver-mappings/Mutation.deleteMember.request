{
    "version" : "2017-02-28",
    "operation" : "DeleteItem",
    "key" : {
        ## If your table's hash key is not named 'id', update it here. **
        "account_id" : { "S" : "${ctx.args.input.account_id}" },
        ## If your table has a sort key, add it as an item here. **
        "user_id" : { "S" : "${ctx.args.input.user_id}" }
    }
}